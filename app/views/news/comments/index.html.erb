
  <%= render 'new_form' %>
<br>
<br>

<% @comments.each do |comments| %>
	<div class="card">
	  <div class="card-body">
	    <h5 class="card-title"><%= link_to comments.user.name, profile_index_path(comments.user.nickname) %></h5>
	    <h6 class="card-title"><%= time_ago_in_words comments.created_at %> atrás</h6>
	    <p class="card-text"><%= comments.content %></p>
	    <% if comments.user == current_user %>
	    <%= link_to comments_index_path(comments), class: "card-link", method: :delete,  data: {confirm: "Excluir este comentario?"} do %>
	    	<i class="fas fa-trash"></i>
	    <% end %>
	    <% end %>
	  </div>
	</div>
	<br>
<% end %>