<div class="container">
<h2>Editar <%= current_user.email %></h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <div class="names_email" style="display: flex;">
    <div class="form-floating">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"%>
    </div>

    <div class="form-floating">
      <%= f.label :nome %><br />
      <%= f.text_field :name, autofocus: true, class: "form-control"%>
    </div>

    <div class="form-floating">
      <%= f.label :nickname %><br />
      <%= f.text_field :nickname, autofocus: true, class: "form-control"%>
    </div>
  
    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>
  </div>
    <div class="senhas" style="display: flex;">

    <div class="form-floating">
      <%= f.password_field :password, autocomplete: "new-password", class: "form-control"%>
      <%= f.label :Nova_Senha %> 
    </div>

    <div class="form-floating">
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control"%>
      <%= f.label :Confirmar_senha %>
    </div>
  </div>
  <em>(Deixe em branco se não quiser alterá-lo)</em>

    <div class="form-floating">
      <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control"%>
      <%= f.label :Senha_atual %> <i>(Precisamos de sua senha atual para confirmar suas alterações)</i><br />
    </div>

    <div class="form-floating">
      <%= f.label :bio %><br />
      <%= f.text_area :bio, autofocus: true, class: "form-control", style: "height: 190px" %>
    </div>
    <div class="sociais" style="display: flex;">
      <div class="form-floating">
        <%= f.text_field :facebook, autofocus: true, class: "form-control"%>
        <%= f.label :facebook %><br />
      </div>

      <div class="form-floating">
        <%= f.text_field :instagram, autofocus: true, class: "form-control"%>
        <%= f.label :instagram %><br />
      </div>

      <div class="form-floating">
        <%= f.text_field :twitter, autofocus: true, class: "form-control"%>
        <%= f.label :twitter %><br />
      </div>
  </div>
  (Adicione o link das suas rede sociais)
    <div class="actions">
      <%= f.submit "Update", class: "btn btn-primary"%>
    </div>
  <% end %>
  <%= link_to "Deletar conta", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete , class: "btn btn-danger"%>
    <%= link_to "Voltar", :back, class: "btn btn-success"%>
</div>
<br>